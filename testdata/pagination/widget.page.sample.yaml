kind: Page
apiVersion: widgets.templates.krateo.io/v1beta1
metadata:
  name: simple-page
  namespace: demo-system
spec:
  widgetData:
    items:
      - resourceRefId: simple-page
  resourcesRefs:
    items:
      - id: pods-buttons-page
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: pods-buttons
        namespace: demo-system
        resource: buttons
        verb: GET
        slice:
          page: 1
          perPage: 3