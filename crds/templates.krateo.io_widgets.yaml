---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.5
  name: widgets.templates.krateo.io
spec:
  group: templates.krateo.io
  names:
    categories:
    - krateo
    - widgets
    kind: Widget
    listKind: WidgetList
    plural: widgets
    shortNames:
    - wdg
    singular: widget
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: Widget is ui widgets configuration.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: WidgetSpec defines widget specifications.
            properties:
              actions:
                description: Actions is an array of actions.
                items:
                  properties:
                    iterator:
                      type: string
                    template:
                      description: ActionTemplate defines a template for an action.
                      properties:
                        apiVersion:
                          description: APIVersion for the related resource
                          type: string
                        id:
                          description: ID for the action.
                          type: string
                        name:
                          description: Name of the related resource.
                          type: string
                        namespace:
                          description: Namespace of the related resource.
                          type: string
                        payloadToOverride:
                          description: PayloadToOverride a list of values to override.
                          items:
                            description: Data is a key value pair.
                            properties:
                              asString:
                                description: AsString if true the value will be considered
                                  verbatim as string.
                                type: boolean
                              name:
                                description: Name of the data
                                type: string
                              value:
                                description: Value of the data. Can be also a JQ expression.
                                type: string
                            required:
                            - name
                            type: object
                          type: array
                          x-kubernetes-list-type: atomic
                        resource:
                          description: Resource on which the action will act.
                          type: string
                        verb:
                          description: Verb is the HTTP request verb.
                          type: string
                      type: object
                  type: object
                type: array
                x-kubernetes-list-type: atomic
              api:
                description: API array of api calls.
                items:
                  description: API represents a request to an HTTP service
                  properties:
                    dependsOn:
                      description: DependsOn reference to another API on which this
                        depends
                      properties:
                        iterator:
                          description: Iterator defines a field on which iterate.
                          type: string
                        name:
                          description: Name of another API on which this depends
                          type: string
                      required:
                      - name
                      type: object
                    endpointRef:
                      description: EndpointRef a reference to an Endpoint
                      properties:
                        name:
                          description: Name of the referenced object.
                          type: string
                        namespace:
                          description: Namespace of the referenced object.
                          type: string
                      required:
                      - name
                      - namespace
                      type: object
                    filter:
                      type: string
                    headers:
                      description: Headers is an array of custom request headers
                      items:
                        type: string
                      type: array
                      x-kubernetes-list-type: atomic
                    name:
                      description: Name is a (unique) identifier
                      type: string
                    path:
                      description: Path is the request URI path
                      type: string
                    payload:
                      description: Payload is the request body
                      type: string
                    verb:
                      description: Verb is the request method (GET if omitted)
                      type: string
                  required:
                  - name
                  type: object
                type: array
                x-kubernetes-list-type: atomic
              app:
                description: App is the app template
                properties:
                  iterator:
                    description: Iterator defines a field on which iterate.
                    type: string
                  template:
                    additionalProperties:
                      type: string
                    description: Template to use during iteration.
                    type: object
                type: object
              propsRef:
                description: PropsRef reference to a config map of extra properties.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  namespace:
                    description: Namespace of the referenced object.
                    type: string
                required:
                - name
                - namespace
                type: object
              type:
                description: Type of this object.
                type: string
            required:
            - type
            type: object
          status:
            description: WidgetStatus contains the Widget results.
            properties:
              items:
                description: Items array of expanded widget details.
                items:
                  description: WidgetResult contains all widget details
                  properties:
                    actions:
                      description: Actions is the array of all available actions.
                      items:
                        description: ActionResult defines the action result after
                          evaluating a template.
                        properties:
                          id:
                            description: ID of this action.
                            type: string
                          path:
                            description: Path is the HTTP request path.
                            type: string
                          payload:
                            description: Payload the payload for the action result
                            properties:
                              apiVersion:
                                type: string
                              kind:
                                type: string
                              metadata:
                                description: Reference to a named object.
                                properties:
                                  name:
                                    description: Name of the referenced object.
                                    type: string
                                  namespace:
                                    description: Namespace of the referenced object.
                                    type: string
                                required:
                                - name
                                - namespace
                                type: object
                            type: object
                          payloadToOverride:
                            description: PayloadToOverride a list of values to override.
                            items:
                              description: Data is a key value pair.
                              properties:
                                asString:
                                  description: AsString if true the value will be
                                    considered verbatim as string.
                                  type: boolean
                                name:
                                  description: Name of the data
                                  type: string
                                value:
                                  description: Value of the data. Can be also a JQ
                                    expression.
                                  type: string
                              required:
                              - name
                              type: object
                            type: array
                            x-kubernetes-list-type: atomic
                          verb:
                            description: Verb is the HTTP request verb.
                            type: string
                        type: object
                      type: array
                      x-kubernetes-list-type: atomic
                    app:
                      additionalProperties:
                        type: string
                      description: Traits is the expandend map of the widget properties.
                      type: object
                  type: object
                type: array
                x-kubernetes-list-type: atomic
              name:
                description: Name user defined name of this object.
                type: string
              props:
                additionalProperties:
                  type: string
                description: Props are user defined extra attributes.
                type: object
              type:
                description: Type of this object.
                type: string
              uid:
                description: UID is the uinique identifier of this object.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
