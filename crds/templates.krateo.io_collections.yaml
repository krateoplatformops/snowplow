---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.5
  name: collections.templates.krateo.io
spec:
  group: templates.krateo.io
  names:
    categories:
    - krateo
    - collections
    kind: Collection
    listKind: CollectionList
    plural: collections
    shortNames:
    - coll
    singular: collection
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: CollectionSpec defines specifications for a Collection.
            properties:
              api:
                description: API array of api calls.
                items:
                  description: API represents a request to an HTTP service
                  properties:
                    dependOn:
                      description: DependOn reference to the identifier (name) of
                        another API on which this depends
                      type: string
                    endpointRef:
                      description: EndpointRef a reference to an Endpoint
                      properties:
                        name:
                          description: Name of the referenced object.
                          type: string
                        namespace:
                          description: Namespace of the referenced object.
                          type: string
                      required:
                      - name
                      - namespace
                      type: object
                    headers:
                      description: Headers is an array of custom request headers
                      items:
                        type: string
                      type: array
                      x-kubernetes-list-type: atomic
                    name:
                      description: Name is a (unique) identifier
                      type: string
                    path:
                      description: Path is the request URI path
                      type: string
                    payload:
                      description: Payload is the request body
                      type: string
                    verb:
                      description: Verb is the request method (GET if omitted)
                      type: string
                  required:
                  - name
                  type: object
                type: array
                x-kubernetes-list-type: atomic
              propsRef:
                description: PropsRef reference to a config map of extra properties
                  for this Collection.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  namespace:
                    description: Namespace of the referenced object.
                    type: string
                required:
                - name
                - namespace
                type: object
              templateIterators:
                description: TemplateIterators array of template iterator references.
                items:
                  properties:
                    iterator:
                      description: Iterator defines a field on which iterate.
                      type: string
                    template:
                      description: Template to use during iteration.
                      properties:
                        apiVersion:
                          type: string
                        name:
                          description: Name of the referenced object.
                          type: string
                        namespace:
                          description: Namespace of the referenced object.
                          type: string
                        resource:
                          type: string
                      required:
                      - name
                      - namespace
                      type: object
                  type: object
                type: array
                x-kubernetes-list-type: atomic
              type:
                description: Type of this object.
                type: string
            required:
            - type
            type: object
          status:
            description: CollectionStatus defines a status for this CollectionIterator.
            properties:
              items:
                description: Items array of expanded widgets.
                items:
                  type: object
                  x-kubernetes-preserve-unknown-fields: true
                type: array
                x-kubernetes-list-type: atomic
              name:
                description: Name user defined name of this collection.
                type: string
              props:
                additionalProperties:
                  type: string
                description: Props are user defined extra attributes.
                type: object
              type:
                description: Type of this collection.
                type: string
              uid:
                description: UID is the uinique identifier of this object.
                type: string
            type: object
        type: object
        x-kubernetes-map-type: atomic
    served: true
    storage: true
    subresources:
      status: {}
